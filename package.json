{
  "private": true,
  "name": "maze-escape",
  "version": "0.0.0",
  "license": "BSD-3-Clause",
  "type": "module",
  "scripts": {
    "build": "esbuild src/web-view/index.ts --bundle --format=esm --log-level=info --outfile=webroot/index.js",
    "clean": "rm -rf dist webroot/index.js",
    "playtest": "devvit playtest --log-runtime $SUBREDDIT"
  },
  "dependencies": {
    "@devvit/public-api": "0.11.4",
    "pixi.js": "^8.6.5"
  },
  "devDependencies": {
    "typescript": "5.3.2",
    "vite": "^6.0.3"
  }
}
